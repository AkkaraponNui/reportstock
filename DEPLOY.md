# การนำขึ้นออนไลน์

deploy ได้ครับ และมีตัวเลือกฟรีที่ใช้ได้จริง แต่มีข้อควรรู้หนึ่งข้อที่ควรอ่านก่อนตัดสินใจ
เพราะมันเปลี่ยนคำตอบว่าควรเลือกแบบไหน

## สรุปสั้น

| สิ่งที่อยากได้ | ควรเลือก |
|---|---|
| ง่ายที่สุด กดไม่กี่ครั้งก็ได้ลิงก์ | Streamlit Community Cloud |
| RAM เยอะ ตั้งเป็นส่วนตัวได้ฟรี | Hugging Face Spaces |
| ข้อมูลไม่หาย และดึงข้อมูลได้เสถียรที่สุด | รันบนเครื่องตัวเองแล้วเปิดผ่าน Cloudflare Tunnel |

ถ้าจะให้ผมเลือกให้ตัวเดียว สำหรับการใช้งานส่วนตัวแบบนี้ผมแนะนำข้อสุดท้าย เหตุผลอยู่ด้านล่าง

## ทรัพยากรที่ระบบนี้ใช้จริง

ผมวัดจริงจากเครื่องนี้ ไม่ใช่ประมาณเอา

| รายการ | ค่าที่วัดได้ |
|---|---|
| หน่วยความจำ ตอนเปิดเซิร์ฟเวอร์เฉยๆ | 76 MB |
| หน่วยความจำ หลังโหลดไลบรารีทั้งหมด | 135 MB |
| หน่วยความจำ สูงสุด หลังดึงข้อมูล 4 ตัวและคำนวณคะแนน | 147 MB |
| จำนวน dependency | 53 แพ็กเกจ |
| ข้อมูลบนดิสก์ ตอนนี้ 12 หุ้น | 812 KB |

กินทรัพยากรน้อยมาก ทุกแพลตฟอร์มฟรีรับไหวสบาย เรื่องขนาดไม่ใช่ปัญหาเลย

## ข้อควรรู้ที่สำคัญที่สุด

**Yahoo Finance มักบล็อก IP ของศูนย์ข้อมูล** แหล่งข้อมูลงบการเงินทั้งหมดของระบบนี้มาจาก Yahoo ผ่าน yfinance
ซึ่งไม่มี API key และไม่มีสัญญาการใช้งาน เมื่อรันจากเครื่องที่บ้านหรือที่ทำงาน IP เป็นแบบ residential จึงแทบไม่มีปัญหา
แต่เมื่อรันจากเซิร์ฟเวอร์ของ Streamlit Cloud, Hugging Face, Render หรือ AWS
IP เหล่านั้นอยู่ในช่วงที่ Yahoo จำกัดอัตราอย่างเข้มงวด และบางครั้งบล็อกทั้งช่วง

อาการที่จะเจอคือดึงข้อมูลแล้วได้ error 429 หรือ 401 หรือได้ข้อมูลว่างเปล่าโดยไม่มี error

ผมทดสอบเรื่องนี้จากเครื่องนี้ไม่ได้ เพราะเครื่องนี้ใช้ IP แบบ residential อยู่แล้ว
จึงบอกได้แค่ว่านี่เป็นความเสี่ยงที่รู้กันทั่วไปในหมู่คนใช้ yfinance ไม่ใช่สิ่งที่ผมยืนยันด้วยตัวเอง

ทางแก้ถ้าเจอปัญหานี้

- ลดความถี่การดึง และพึ่ง cache ให้มากขึ้น ระบบ cache ผลค้นหาไว้ 1 ชั่วโมงอยู่แล้ว
- ดึงข้อมูลบนเครื่องตัวเอง แล้ว commit โฟลเดอร์ `data/` ขึ้น repo ให้แอปบนคลาวด์อ่านอย่างเดียว
- สมัคร API key ฟรีของ Finnhub หรือ Alpha Vantage ซึ่งเครื่องมือข่าวรองรับอยู่แล้ว แต่ส่วนงบการเงินยังต้องพึ่ง Yahoo

**ไฟล์ข้อมูลจะหายเมื่อแอปรีสตาร์ท** แพลตฟอร์มฟรีทุกเจ้าใช้ระบบไฟล์แบบชั่วคราว
เมื่อแอปหลับแล้วตื่น หรือ deploy ใหม่ โฟลเดอร์ `data/` จะว่างเปล่า

เรื่องนี้ขัดกับหลักการออกแบบของระบบโดยตรง snapshot ที่ลงวันที่ไว้และไม่เขียนทับข้ามวัน
มีไว้เพื่อให้ย้อนกลับไปตรวจได้ว่าข้อสรุปในอดีตสมเหตุสมผลกับข้อมูลที่มีอยู่ ณ เวลานั้นหรือไม่
พอไฟล์หายทุกครั้งที่รีสตาร์ท มันก็เหลือสถานะแค่ cache ไม่ใช่หลักฐานย้อนหลังอีกต่อไป

ถ้ายอมรับได้ก็ deploy ได้เลย ถ้ารับไม่ได้ ให้ commit `data/` ขึ้น repo หรือรันบนเครื่องตัวเอง

**ลิงก์สาธารณะหมายถึงใครก็กดดึงข้อมูลได้** ทุกครั้งที่มีคนกดปุ่มดึงข้อมูล มันคือการยิงคำขอไปที่ Yahoo และ Google News
ในนามของคุณ ผมจึงใส่ระบบรหัสผ่านไว้ให้แล้ว ตั้งค่า `APP_PASSWORD` เมื่อไหร่ ประตูจะเปิดทำงานทันที
ถ้าไม่ตั้ง ระบบจะไม่ถามรหัส ซึ่งเหมาะกับการรันบนเครื่องตัวเอง

ควรตั้งรหัสผ่านเสมอเมื่อ deploy ขึ้นที่สาธารณะ

## วิธีที่ 1 Streamlit Community Cloud

ง่ายที่สุด ฟรี ไม่ต้องใช้บัตรเครดิต ทำมาเพื่อ Streamlit โดยเฉพาะ

1. push โค้ดขึ้น GitHub แผนฟรีรองรับ repo ส่วนตัวได้ 1 แอป
2. เข้า share.streamlit.io แล้วล็อกอินด้วย GitHub
3. กด New app เลือก repo เลือก branch ตั้ง Main file path เป็น `app.py`
4. กด Advanced settings เลือก Python version เป็น 3.11
5. ในช่อง Secrets ใส่

   ```toml
   REPORTSTOCK_UA = "ชื่อคุณ you@example.com"
   APP_PASSWORD = "ตั้งรหัสที่เดายาก"
   ```

6. กด Deploy รอสักครู่จะได้ลิงก์

ใช้ `requirements.txt` ที่เตรียมไว้ให้แล้ว ไม่ต้องแตะ Dockerfile

ข้อจำกัด แอปจะหลับหลังไม่มีคนใช้ราว 12 ชั่วโมง และตื่นเมื่อมีคนเปิด ซึ่งใช้เวลาสักพัก
และข้อมูลใน `data/` หายทุกครั้งที่หลับแล้วตื่น

## วิธีที่ 2 Hugging Face Spaces

RAM มากกว่า และตั้งเป็น private ได้ฟรี เหมาะถ้าไม่อยากให้ใครเห็น

1. สมัคร huggingface.co แล้วกด New Space
2. ตั้ง Space SDK เป็น Streamlit และเลือก Private ถ้าต้องการ
3. push ไฟล์ทั้งหมดขึ้น Space repo
4. เพิ่ม frontmatter นี้ไว้บนสุดของ `README.md` ใน Space

   ```yaml
   ---
   title: reportstock
   sdk: streamlit
   sdk_version: "1.64.0"
   app_file: app.py
   pinned: false
   ---
   ```

5. ไปที่ Settings แล้วใส่ `REPORTSTOCK_UA` และ `APP_PASSWORD` ในส่วน Secrets

ถ้าอยากคุมสภาพแวดล้อมเองให้เลือก SDK เป็น Docker แล้วใช้ `Dockerfile` ที่เตรียมไว้
ไฟล์นั้นตั้งพอร์ตเป็น 7860 ตามที่ Spaces ต้องการไว้แล้ว

หมายเหตุ ผมยังไม่ได้ทดสอบ build จาก Dockerfile จริง เพราะ Docker daemon บนเครื่องนี้ไม่ได้รันอยู่
ไฟล์เขียนตามรูปแบบมาตรฐานแต่ยังไม่ผ่านการยืนยันด้วยการ build

## วิธีที่ 3 รันบนเครื่องตัวเอง แล้วเปิดผ่าน Cloudflare Tunnel

วิธีนี้แก้ปัญหาทั้งสองข้อข้างบนพร้อมกัน ข้อมูลอยู่บนเครื่องคุณจึงไม่หาย
และคำขอออกจาก IP บ้านคุณ Yahoo จึงไม่บล็อก

```bash
uv run python -m streamlit run app.py
# อีกหน้าต่างหนึ่ง
cloudflared tunnel --url http://localhost:8501
```

จะได้ลิงก์ `https://xxxx.trycloudflare.com` ที่เปิดจากมือถือหรือที่อื่นได้ทันที
ไม่ต้องสมัครบัญชี ไม่ต้องตั้งค่า router

ข้อแลกเปลี่ยนคือเครื่องต้องเปิดอยู่ และลิงก์แบบ quick tunnel จะเปลี่ยนทุกครั้งที่รันใหม่
ถ้าอยากได้ลิงก์ถาวรต้องสมัครบัญชี Cloudflare ซึ่งก็ฟรีเช่นกัน

ต้องตั้ง `APP_PASSWORD` ด้วย เพราะลิงก์ trycloudflare เปิดสาธารณะ

## เรื่องลิขสิทธิ์ข้อมูล

ข้อมูลจาก Yahoo Finance มีเงื่อนไขการใช้งานที่อนุญาตการใช้ส่วนตัว
แต่ไม่ได้อนุญาตให้นำไปเผยแพร่ต่อสาธารณะอย่างชัดเจน การ deploy แบบส่วนตัวหรือมีรหัสผ่านจึงปลอดภัยกว่า
ทั้งในแง่กฎหมายและในแง่ไม่ให้คนอื่นมาใช้โควตาของคุณหมด

Google News RSS และ SEC EDGAR ใช้ได้อิสระกว่า แต่ EDGAR ขอให้ระบุ User-Agent ที่ติดต่อกลับได้จริง
ซึ่งคือค่า `REPORTSTOCK_UA` ที่ต้องตั้ง

## ช่องแชทกับการ deploy

ช่องแชทเป็นหน้าเดียวที่ต้องใช้กุญแจ API และมีค่าใช้จ่ายจริงตามปริมาณการใช้งาน
เมื่อ deploy ขึ้นที่สาธารณะ ทุกคนที่เปิดลิงก์ได้จะใช้โควตาของคุณ

ใส่กุญแจในช่อง Secrets ของแพลตฟอร์ม ไม่ใช่ในโค้ด

```toml
ANTHROPIC_API_KEY = "sk-ant-..."
APP_PASSWORD = "ตั้งรหัสที่เดายาก"
```

ถ้าไม่อยากเปิดช่องแชทบนเวอร์ชันออนไลน์ ก็แค่ไม่ต้องใส่กุญแจ หน้าอื่นทั้งหมดยังทำงานปกติ
และหน้าแชทจะแสดงวิธีตั้งค่าแทนที่จะพัง

การตั้ง `APP_PASSWORD` สำคัญเป็นพิเศษเมื่อเปิดช่องแชท เพราะค่าใช้จ่ายผูกกับบัญชีคุณโดยตรง

## ก่อน deploy ทุกครั้ง

- ตั้ง `APP_PASSWORD` ถ้าลิงก์เข้าถึงได้จากภายนอก
- ตั้ง `REPORTSTOCK_UA` เป็นชื่อและอีเมลจริง ไม่งั้น EDGAR จะจำกัดการเข้าถึง
- อย่า commit ไฟล์ `.env` หรือ `.streamlit/secrets.toml` ทั้งสองถูกใส่ใน `.gitignore` ไว้แล้ว
- ถ้าอยากให้แอปบนคลาวด์มีข้อมูลตั้งต้น ให้ commit โฟลเดอร์ `data/` ขึ้นไปด้วย
  ข้อมูลในนั้นเป็นข้อมูลสาธารณะทั้งหมด ไม่มีความลับ

## อัปเดต requirements.txt เมื่อเพิ่มไลบรารี

```bash
uv export --no-hashes --no-dev --no-annotate --no-header --format requirements-txt > requirements.txt
```
